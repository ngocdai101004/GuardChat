# Llama-3.1-8B-Instruct Task-2 rewriter (src/Llama).
#
#   pip install -r src/Llama/requirements.txt
#
# transformers >= 4.43 is the first release with the Llama 3.1
# architecture (rope_scaling type "llama3"); older versions fail to load
# the config. bitsandbytes is only needed for --dtype int8 / nf4 and is
# CUDA-only.

torch>=2.1
transformers>=4.43,<5
accelerate>=0.26
huggingface_hub>=0.23
tqdm>=4.65
# bitsandbytes>=0.43   # uncomment for --dtype int8 / nf4 (CUDA only)
